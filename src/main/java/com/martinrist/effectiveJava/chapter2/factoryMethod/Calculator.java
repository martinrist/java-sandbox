package com.martinrist.effectiveJava.chapter2.factoryMethod;

/**
 * Trivial interface for a calculator, that contains typical calculation methods.
 */
public interface Calculator {

    /**
     * Adds two integers, returning their sum.
     * @param x
     * @param y
     * @return x + y
     */
    int add(int x, int y);

}
